import { CalendarEntry } from "../types/calendarLog";

/**
 * Initial calendar entries for EBB Lab Work Log.
 * Default is empty for fresh administrative usage.
 */
export const initialCalendarEntries: CalendarEntry[] = [];
